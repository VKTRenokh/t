import { FilterByConnectionPipe } from './filter-by-connection.pipe';

describe('FilterByConnectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByConnectionPipe();
    expect(pipe).toBeTruthy();
  });
});
