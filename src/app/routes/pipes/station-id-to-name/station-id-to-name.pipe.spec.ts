import { StationIdToNamePipe } from './station-id-to-name.pipe';

describe('StationIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new StationIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
