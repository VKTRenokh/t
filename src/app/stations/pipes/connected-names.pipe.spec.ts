import { ConnectedNamesPipe } from './connected-names.pipe';

describe('ConnectedNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConnectedNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
